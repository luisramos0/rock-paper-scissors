#!/bin/sh
if [ -x "`dirname "$0"`/bash" ]; then
  "`dirname "$0"`/bash"  "`dirname "$0"`/../mocha/bin/mocha-debug" "$@"
  ret=$?
else 
  bash  "`dirname "$0"`/../mocha/bin/mocha-debug" "$@"
  ret=$?
fi
exit $ret
